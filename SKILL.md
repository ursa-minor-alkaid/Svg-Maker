---
name: "svg-maker"
description: "专业 SVG 生成器。当用户说\"帮我生成 SVG\"\"画一个 SVG\"\"create an SVG\"\"draw SVG\"\"制作矢量图\" \"制作svg\"或类似表达时调用。"
---

# SVG Maker — 专业 SVG 矢量图生成器

将用户需求转化为高质量、严谨、符合要求的 SVG 代码。

---

## 工作流程

### 第一步：需求判断与分类

根据用户输入的明确程度，将需求分为两类：

| 类别 | 特征 | 处理方式 |
|------|------|----------|
| **模糊需求** | 仅给出主体名称或者核心思想，如"画一只黑猫""生成一个火箭 SVG""生成一个用于展示、具象化项目核心原则的、富有设计感、艺术感的简洁的 icon svg"| 先搜索网络获取参考，自行撰写详细提示词，再生成 |
| **明确需求** | 给出了非常非常详细的提示词和细节描绘 | 直接进入结构化提示词生成阶段 |

**注意：任何字数小于60字/90tokens的需求，都视为模糊需求**

---

### 第二步：模糊需求处理（仅模糊需求时执行）

当用户仅给出笼统描述时，按以下步骤处理：

1. **WebSearch**：
   - 搜索该主体的视觉特征描述、结构拆解、已有代码参考
   - **使用多种提示词组合进行多次的、全方位的搜索**
   - 详细的搜索及发返回提示词详见：[搜索提示词](./references/SearchPrompts.md)（路径：`./references/SearchPrompts.md`）
2. **向用户展示、并寻求反馈及确认**
   - 将当前的搜索结果汇总后，简要地告诉用户绘制思路，并询问是否符合预期、是否有信息需要补充
   - 若用户补充了较多信息，则重新搜索并更新搜索结果
   - **将前述搜索结果和用户补充信息汇总整合，进入结构化提示词生成步骤**

---

### 第三步：结构化提示词生成

无论需求类型，在生成 SVG 代码前，必须先在内部（思维链）按以下四个层次组织提示词。该过程对齐人类画师的构图逻辑：先构思场景，再拆解对象，再规划布局与绘制顺序，最后确定技术参数。

#### 1. 场景级扩展（Scene-level）
将需求转化为具体的画面描述，聚焦"画面中出现哪些对象"，而非停留在抽象概念：
- 识别必须出现的核心对象，并为每个对象补充视觉细节（颜色、大小、形状、姿态、状态）
- 克制地引入辅助元素：仅当能明显增强场景完整性时才添加；单一主体场景可专注于丰富主体细节，不必强行扩充对象

#### 2. 对象级分解（Object-level）
将每个对象拆解为可被 SVG 基础元素表达的组件：
- **完整列举所有关键部件**，包括常识性部件——如画猫必须包含耳朵、眼睛、胡须、尾巴、四肢，即使需求未提及；被遗漏的部件将视为不存在于画面
- 每个部件标注对应的 SVG 元素类型（`<rect>` / `<circle>` / `<ellipse>` / `<line>` / `<polygon>` / `<polyline>` / `<text>` / `<g>`）
- 用最少的部件实现目标效果，避免冗余图元

#### 3. 布局级规划（Layout-level）
为每个部件规划可直接翻译为代码的精确参数：
- 在 viewBox 范围内分配坐标区域，明确各部件的坐标与尺寸（如"椭圆主体，中心 (256, 256)，rx=90、ry=60，粉色"）
- 规划**绘制顺序序列**：SVG 按代码顺序渲染、后定义者位于上层，故按"背景 → 主体 → 细节/前景"排序
- 标注部件间的连接/从属关系（如颈部衔接头部与身体）与遮挡关系，确保相邻部件坐标对齐、不悬空断裂
- 被其他图层完全包围的部件须使用可区分的颜色，防止渲染后不可见

#### 4. 技术规范定义
必须明确以下参数：
- **viewBox** 尺寸（如 `viewBox="0 0 800 600"`）
- **允许元素集**：优先 `<rect>`, `<circle>`, `<ellipse>`, `<line>`, `<text>`, `<g>`，用基础形状组合替代 `<path>`；确有必要时允许短 `<path>`（≤5 条命令，以 `Z` 结尾）
- **颜色方案**：十六进制色值，明确背景色、主体色、强调色的分配
- **字体**：标准无衬线系统字体（`font-family="system-ui, sans-serif"`）
- **代码规范**：每个元素添加 `id` 属性和 `<!-- 注释 -->`，用 `<g>` 分组管理相关元素；圆角用 `rx` 属性，不用贝塞尔路径；禁止使用渐变、滤镜、裁剪路径（除非用户明确要求）

---

### 第四步：SVG 代码生成规则

将第三步的规划**逐条翻译**为代码：布局规划中的每个部件、每个参数（坐标、尺寸、颜色、顺序）都应有对应元素，不即兴发挥、不遗漏。生成时遵守：

1. **元素与结构**：
   - 仅使用第三步定义的允许元素集；`<path>` 仅限短路径（≤5 条命令、以 `Z` 结尾），只含单条命令的路径须加大 `stroke-width` 保证可见
   - 严格按规划的绘制顺序书写：背景最先，后定义的元素位于上层
   - 相关元素用 `<g>` 分组并添加语义注释，关键元素添加 `id`
2. **色彩规范**：
   - 所有颜色使用十六进制色值（如 `#4361ee`, `#2b2d42`）
   - 无背景时避免纯白形状（防止白色背景上看不见）；被包围图层与外层颜色须可区分
3. **文本规范**：`<text>` 元素必须明确 `x`, `y`, `font-size`, `fill` 属性，字体用 `system-ui, sans-serif`
4. **生成后自检**：交付前在脑中"渲染"画面，逐项核对：
   - 部件完整：第三步列举的部件是否全部出现
   - 连接对齐：相邻部件坐标衔接，无悬空断裂
   - 比例合理：各部件相对尺寸符合常识，不出现头大身小等失衡
   - 叠放正确：前景未被背景遮挡，被包围部件颜色可辨
5. **渲染兼容性**：确保代码可在主流浏览器中直接打开渲染，不生成无效属性，不依赖外部资源

---

### 第五步：输出与迭代

#### 首次输出
将生成的 SVG 代码保存到工作区，文件命名格式为 `<主体描述>_<日期（如，260925）>.svg`。

#### 迭代修正
如果用户提出修改意见，**针对性修改单一问题，不要整体重绘**：
- "把某个矩形的颜色改为 #xxx"
- "将某段文字上移 20 个单位"
- "把箭头从实线改为虚线"
- 每次修改后重新保存文件（覆盖或新版本均可）

