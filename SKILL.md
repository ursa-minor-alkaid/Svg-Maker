---
name: "svg-maker"
description: "专业 SVG 生成器。当用户说\"帮我生成 SVG\"\"画一个 SVG\"\"create an SVG\"\"draw SVG\"\"制作矢量图\" \"制作svg\"或类似表达时调用。"
---

# SVG Maker — 专业 SVG 矢量图生成器

将用户需求转化为高质量、严谨、符合要求的 SVG 代码

---

## 工作流程

### 第一步：需求判断与分类

根据用户输入的明确程度，将需求分为三类：

| 类别 | 特征 | 处理方式 |
|------|------|----------|
| **文字输入需求** | 纯文字输入生成 svg 的要求，不包含图片 | 参考[文字输入需求工作流](references/InputWoImg.md) |
| **图片输入需求** | **输入中包含图片**，要求根据图片生成 SVG 代码 | 参考[含图片输入需求工作流](references/InputWithImg.md) |

---

### 第二步：参考对应工作文档的要求进行输出

根据需求类型，参考对应文档，严格按照文档的工作流程进行处理

---

### 第三步：检查公式/箭头/布局/中文乱码/跑偏等情况

<important>

完成 SVG 后不要立即交付，必须检查并修正以下项目。

**1. 图意和流程**

- 图是否表达了用户要求的真实方法结构。
- 箭头输入输出是否正确。
- 分支是否在正确位置分流和汇合。
- 加号、融合、合并等节点是否符合用户描述。
- 是否误删了用户要求保留的原图模块。

**2. 线条和箭头**

- 箭头方向是否正确。
- 线条是否横平竖直，是否过度交叉。
- 线条是否穿过模块文字、公式、标题或注释。
- 箭头头部大小是否合适。
- 多条线是否重叠到无法辨认。
- 连线是否接到模块边缘，而不是插进模块内部文字。

**3. 中文和字体**

- 中文是否正常显示，没有乱码。
- 文字是否居中、压线、溢出或跑偏。

</important>

---

### 第四步：输出与迭代

#### 首次输出
将生成的 SVG 代码保存到工作区，文件命名格式为 `<主体描述>_<日期（如，260925）>.svg`。

#### 迭代修正
如果用户提出修改意见，**针对性修改单一问题，不要整体重绘**：
- "把某个矩形的颜色改为 #xxx"
- "将某段文字上移 20 个单位"
- "把箭头从实线改为虚线"
- 每次修改后重新保存文件（覆盖或新版本均可）

