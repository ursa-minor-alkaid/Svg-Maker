---
name: "svg-maker"
description: "专业 SVG 生成器，遵循 Chat2SVG/SVGThinker 最佳实践。当用户说\"帮我生成 SVG\"\"画一个 SVG\"\"create an SVG\"\"draw SVG\"\"制作矢量图\"或类似表达时调用。支持架构图、流程图、图标、信息图、插画等各类 SVG。"
---

# SVG Maker — 专业 SVG 矢量图生成器

基于 Chat2SVG (CVPR 2025)、SVGThinker (ACM 2025) 及业界最佳实践，将用户需求转化为高质量、可渲染的原始 SVG 代码。

---

## 触发条件

当用户发出以下语义的请求时，**必须**主动调用本 Skill：

- "帮我生成一个 SVG" / "画一个 SVG" / "做一个 SVG"
- "create an SVG" / "draw an SVG" / "make an SVG"
- "生成矢量图" / "画矢量图"
- "用 SVG 画一个 [xxx]"
- "把 [xxx] 做成 SVG"
- 以及其他任何要求以 SVG 格式输出图形的场景

---

## 工作流程

### 第一步：需求判断与分类

根据用户输入的明确程度，将需求分为两类：

| 类别 | 特征 | 处理方式 |
|------|------|----------|
| **模糊需求** | 仅给出主体名称，如"画一只黑猫""生成一个火箭 SVG" | 先搜索网络获取参考，自行撰写详细提示词，再生成 |
| **明确需求** | 描述了结构、风格、颜色、元素，如"画一个包含用户层、网关层、服务层的三层架构图" | 直接进入结构化提示词生成阶段 |

---

### 第二步：模糊需求处理（仅模糊需求时执行）

当用户仅给出笼统描述时，按以下步骤处理：

1. **WebSearch**：搜索该主体的视觉特征描述、结构拆解、常见风格参考
   - 搜索关键词示例：`"[主体名称] SVG illustration guide structure components"`
   - 获取主体的关键部件列表、典型配色、姿态特征

2. **撰写详细提示词**：基于搜索结果，自行撰写一份包含以下内容的详细提示词：
   - 主体的所有核心部件及子部件（对象级分解）
   - 各部件的形状类型（circle/rect/ellipse/polygon 等）
   - 各部件的颜色方案（十六进制色值）
   - 各部件的相对位置与大小（布局级规划）
   - 整体风格与氛围描述
   - viewBox 尺寸建议

3. **向用户展示**：将详细提示词展示给用户确认（简要说明构思即可），然后进入生成阶段。

---

### 第三步：结构化提示词生成

无论需求类型，在生成 SVG 代码前，必须先在内部（思维链）按以下四个层次组织提示词：

#### 1. 场景级扩展（Scene-level）
识别画面中必须出现的核心对象，补充增强场景完整性的辅助元素。

#### 2. 对象级分解（Object-level）
将每个对象拆解为可被 SVG 基础元素表达的组件：
- 列出每个部件及其对应的 SVG 元素类型（`<rect>` / `<circle>` / `<ellipse>` / `<line>` / `<polygon>` / `<polyline>` / `<text>` / `<g>`）
- 确保没有关键部分遗漏

#### 3. 布局级规划（Layout-level）
明确各元素的位置、大小、颜色、空间关系与叠放顺序：
- 在 viewBox 范围内分配坐标区域
- 说明前后遮挡关系（后定义的元素在上层）
- 标注各部件的连接/从属关系

#### 4. 技术规范定义
必须明确以下参数：
- **viewBox** 尺寸（如 `viewBox="0 0 800 600"`）
- **允许元素集**：优先 `<rect>`, `<circle>`, `<ellipse>`, `<line>`, `<text>`, `<g>`，尽量用基础形状组合替代复杂 `<path>`
- **颜色方案**：十六进制色值，如背景色、主体色、强调色
- **字体**：标准无衬线系统字体（`font-family="system-ui, sans-serif"`）
- **代码规范**：每个元素添加 `id` 属性和 `<!-- 注释 -->`
- 使用 `<g>` 分组管理相关元素
- 圆角用 `rx` 属性，不用贝塞尔路径
- 禁止使用渐变、滤镜、裁剪路径（除非用户明确要求）

---

### 第四步：SVG 代码生成规则

生成代码时必须遵守以下规则：

1. **结构优先于外观**：用明确的节点、连接、层级描述驱动生成，不要依赖模糊的美学词汇
2. **色彩规范**：
   - 所有颜色使用十六进制色值（如 `#4361ee`, `#2b2d42`）
   - 如需深色模式适配，使用 CSS 变量或 `currentColor`
   - 避免在无背景时使用纯白色形状（防止白色背景上看不见）
3. **代码质量**：
   - 每个 `<g>` 组添加注释说明其语义
   - 关键元素添加 `id` 属性
   - `<text>` 元素必须明确 `x`, `y`, `font-size`, `fill` 属性
   - 后定义的元素在上层，背景必须放在最前面
   - 使用 `<g>` 分组管理相关元素，保持代码结构清晰
4. **避免常见缺陷**：
   - 不使用复杂 `<path>`（用基础形状组合代替）
   - 检查叠放顺序是否正确
   - 确保各部件的相对比例合理
   - 确保连接部位对齐
   - 不生成无效或浏览器不支持的属性
5. **渲染兼容性**：确保代码可在主流浏览器中直接打开渲染，不依赖外部资源

---

### 第五步：输出与迭代

#### 首次输出
将生成的 SVG 代码保存到工作区，文件命名格式为 `<主体描述>_<时间戳>.svg`。

#### 迭代修正
如果用户提出修改意见，**针对性修改单一问题**，**不要整体重绘**：
- "把某个矩形的颜色改为 #xxx"
- "将某段文字上移 20 个单位"
- "把箭头从实线改为虚线"
- 每次修改后重新保存文件（覆盖或新版本均可）

---

## 交互示例

### 示例 1（模糊需求）
> 用户："帮我生成一只黑猫的 SVG"
>
> **你的处理**：
> 1. 判定为模糊需求，触发搜索流程
> 2. WebSearch 搜索 "black cat illustration SVG structure components silhouette"
> 3. 撰写详细提示词："一只坐姿黑猫，viewBox 0 0 800 600，核心部件：椭圆形身体(#1a1a2e)、圆形头部(#1a1a2e)、三角形耳朵(#1a1a2e)、杏仁形眼睛(#ffd166)、细长尾巴(polygon #1a1a2e)、胡须(line #cccccc)。背景为浅灰色圆角矩形(#f5f5f5)。风格为简约扁平插画，使用基础形状组合。"
> 4. 生成 SVG 并保存

### 示例 2（明确需求）
> 用户："画一个三层的微服务架构图 SVG"
>
> **你的处理**：
> 1. 判定为明确需求
> 2. 直接规划结构：网关层→服务层→数据层，自上而下布局，viewBox 0 0 800 900
> 3. 拆解每层模块，定义颜色、位置、连接线
> 4. 生成 SVG 并保存

### 示例 3（迭代修正）
> 用户："把网关层的颜色改成 #4361ee"
>
> **你的处理**：
> 1. 定位网关层相关的 `<rect>` 元素
> 2. 仅修改其 fill 属性
> 3. 保存更新后的文件
